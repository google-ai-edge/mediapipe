file_name: "custom_pose_landmarker_nofilter.task"

# Main pose detector model
model_asset {
  file_name: "models/pose_detector.tflite"
}

# Pose landmarks detector model
model_asset {
  file_name: "models/pose_landmarks_detector.tflite"
}

# Graph configuration
graph_options {
  use_gpu: false  # Set to true for GPU version
  enable_segmentation: false
} 